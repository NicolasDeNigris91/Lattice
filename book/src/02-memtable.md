# The memtable

_Lands with v0.1. This page is a placeholder._

Topics planned, why an ordered in-memory structure is required, the choice
between `BTreeMap` and a skip list, sizing thresholds, and the moment a
memtable becomes immutable and is queued for flush.
