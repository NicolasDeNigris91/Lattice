# The write ahead log

_Lands with v0.1. This page is a placeholder._

Topics planned, the durability contract, append-only files and `fsync`
semantics, the record format with CRC32 checksum, recovery on startup, and
the trade-off between sync-per-write and group commit.
