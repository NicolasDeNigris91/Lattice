# Sorted string tables

_Lands with v0.2. This page is a placeholder._

Topics planned, on-disk layout (data blocks, sparse index, footer with magic
number), block compression with LZ4, and the cost model behind block size
defaults.
